## Running the andro app
```docker build -t <your-tag-name> .``` \
```docker run -p 8019:8018 <tag-name>```

Still need to enable docker-compose functionality
Config server yaml disabled while in development